<div class="container">
    {{#> FormAuth}}
        {{> Input label="Login" }}
        {{> Input label="Password" }}
        {{> Button label="Sign in" type="primary" page="list"}}
        {{> Button label="Sign up" type="link" }}
        {{test}}
    {{/FormAuth}}
</div>
